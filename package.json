{
  "name": "feedl",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "lerna run start",
    "bootstrap": "lerna bootstrap",
    "start:components": "yarn workspace @feedl/components start",
    "start:site": "yarn workspace @feedl/site start",
    "build:site": "yarn workspace @feedl/site build",
    "build:components": "yarn workspace @feedl/components build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.10.5"
  }
}
